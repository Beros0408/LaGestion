import React from "react";
import ReactDOM from "react-dom/client";
import LagestionDashboard from "./LagestionDashboard.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LagestionDashboard />
  </React.StrictMode>
);
